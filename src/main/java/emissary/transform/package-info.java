/**
 * Base Emissary places that transform the data. Transform phase of the workflow is expected to modify the bytes of the
 * payload, set the current form to something new when appropriate, extract nested child payload objects, record
 * metadata.
 */
package emissary.transform;
