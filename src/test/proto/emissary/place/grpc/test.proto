syntax = "proto3";

package emissary.place.grpc;

option java_multiple_files = true;

message TestRequest {
  string query = 1;
}

message TestResponse {
  string result = 1;
}

service TestService {
  rpc Uppercase(TestRequest) returns (TestResponse);
}
