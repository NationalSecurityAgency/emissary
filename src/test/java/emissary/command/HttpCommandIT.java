package emissary.command;

import org.junit.Test;

public class HttpCommandIT {

    @Test
    public void setupHttp() {
        // test that is actually add SSL to flavor when sslEnabled = true
        // test that if you set flavor to SSL and sslEnabled = true, only one SSL flavor is passed on
    }
}
